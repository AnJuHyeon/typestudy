{
    "compilerOptions": {
        "skipLibCheck": true,
        "rootDir": "./src", //소스 파일 안헤서 root 폴더 명시시
        "outDir": "./build/js", //ts 컴파일 후에 어느 경로로 들어가는지 암시
        "target" : "ES2015", //타입스크립트를 ES2015버전의 자바스크립트로 변환시켜줌
        "noEmitOnError": true, //에러가 있을 때는 컴파일하지 않는다.
        "module": "ESNext",
        "moduleResolution": "Node",
        "esModuleInterop": true,
        "lib": ["ESNext","DOM"],
        "strict": true, //타입을 엄격하게 사용한다. any 사용 제한

    },
    "include": [// 소스 파일의 어디에서 타입스크립트 파일을 찾을 수 있는지 명시
        "./src/**/*.ts" //src 폴더 안에 모든 하위 경로, 모든 ts 파일
    ],
    "exclude": [
        "node_modules"
    ]   
}